# CLAUDE.md — Web S

Canonical instructions for all coding agents live in [AGENTS.md](AGENTS.md),
imported below. Edit AGENTS.md, not this file.

The short version: the app this repo ships is the SvelteKit client in `svelte/`.
`src/` is tweb — the MTProto stack it imports, plus the upstream Solid client.

@AGENTS.md
